# Runtime state created by the docker-compose bind mount (config, paired
# clients, app list, TLS certs, logs) — never commit it.
/config/

# Build logs / artifacts.
*.log

# OS / editor cruft.
.DS_Store
Thumbs.db
*.swp
*.txt